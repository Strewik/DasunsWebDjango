# import os
# from dotenv import load_dotenv
# load_dotenv(verbose=True)

# import smtplib

# EMAIL_HOST_USER = 'lilngonian3000@gmail.com'
# EMAIL_HOST_PASSWORD = 'Decentralise3000!'

# with smtplib.SMTP('smtp.gmail.com', 587) as smtp:
#     smtp.ehlo()
#     smtp.starttls()
#     smtp.ehlo()
    
#     smtp.login(EMAIL_HOST_USER, EMAIL_HOST_PASSWORD)
    
#     subject = 'Dasuns application'
#     body = 'Hello, we have received you application'
    
#     msg = f'Subject : {subject}\n\n{body}'
    
#     smtp.sendmail(EMAIL_HOST_USER, kawooyastevenug@gmail.com, msg)
    
    
    
    